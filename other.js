class Formatter {

    // Write a method static capitalize that takes in a string and 
    // capitalizes the first letter.
    static capitalize(string) {
        let x = string[0]
        x.capitalize
        return x
    }

    // Write a method static sanitize that takes in a string and 
    // removes all non-alphanumeric characters except for dashes, 
    // single quotes and spaces.
    static sanitize(string) {
        
    }

// Write a method static titleize that takes in a string and 
 // capitalizes all words in a sentence except the, a, an, but, of, and, for, a
// t, by, and from; and always capitalizes the first word.
    static titleize() {

    }

}




